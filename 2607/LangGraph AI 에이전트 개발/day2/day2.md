# day2

https://bit.ly/li_ai_agent

https://app.tavily.com/home

머메이드 프로그램

### 어제 내용 복습

- 라지? 파라미터?
- 시스템 메세지, 휴먼 메세지 구분은 LLM이 하지 않음(하나의 문자열로 판단함)

- 비즈니스 프로세스(업무프로세스)

- initial_state = {"messages": "오늘 서울 기온은?"}





## AI Agent 도구와 메모리

- tools_condition, toolnode
- bind_tools



- tool을 직접 생성 가능
  - docstring을 자세히 써놔야 LLM이 해당 툴을 필요시 사용할때 참고함(어떤 기능인지 내용으로 판단)
- 컨텍스트엔지니어링
  - LLM이 메시지를 주고받으며 점점 프롬프트가 쌓이는데, 이 중에서 결정을 위한 부분만 적절하게 가져갈 수 있도록 설계하는것(모든 프롬프트를 가지고 판단하게 하면 속도도 느려지고 토큰도 많이 사용하게 됨)
  - 들어오는 전체 문자열 - 컨텍스트?
  -  프롬프트 점점 길어짐(메모리 등으로 저장한다면) - 문자열로 전달됨 -> 이 대화 전체 내역이란 관점으로 본다면 하나의 맥락(컨텍스트)
  - 컨텍스트를 효율적으로 관리하기 위해 멀티에이전트로도 구현되기도 함(각 필요한 기능 별로 나눌때, 핵심 컨텍스트만 가지도록 최적화 가능하므로)



#### 메모리

- 단기메모리
  - 새창, ID, 쓰레드(id?)
  - 윈도우 메모리, 요약메모리(LLM 확인해서)



- text to sql 
  - 작년기준 생성형 AI를 파인튜닝
  - 에이전트로 만들어서 text to sql





## RAG 개념 이해

- 대규모 텍스트 데이터 LLM with RAG  절차 및 임베딩 다시 벡터를 살펴보기 	▪ ▪ ▪ ▪ ①  사용자  • 임베딩  ②  질문을  :  벡터로  받음  변환 준비된  정보  DB ( 질문  에서  벡터 ) 답변에  • [ 질문  벡터 ] 와  DB 내  저장된  [ 필요한  문서  • 가장  유사도가  ③  필요한  ④  LLM 이  높은  문서  문서를  n 개  포함한  찾기 프롬프트  답변  생성하기 문서  벡터 ] 와  유사도  생성 저장 검색 계산



#### vector DB 구축 절차

- 벡터 db에서의 embed - 자연어를 기계어로 변환(숫자)
- 전체 데이터에 대해 어느단위로 쪼갤지는 정답이 없고 개발자가 구축 시스템 목적에 맞게 찾아야함(노가다, 경우의 수를 다 실험해봄)



- 텍스트를 숫자화 하는건 이미 임베드 유명 모델 및 연산식이 있어서 해당 식을 활용하면됨 (openaiEmbeddings 등)



- 벡터 길이? norm 노름,노엄?
- 벡터의 내적이 AI에서 어떤 의미를 지니는가?
  - 유사도 계산?, 얼마나 두 벡터 간 가까운지 확인하는거와 유사
- 유사도 계산(벡터)
  - 거리 - 유클리드 거리(L2 유사도) cos = b/a
  - 각도 - 코사인 유사도

- L2 거리에 루트까지 제거해서 계산하면 속도가 빨라서 최근에는 해당 식으로 계산하는게 대세



- pdf는 별도 split이 필요할 수 있으나
- csv는 보통 행단위로 이미 쪼개져 있어서 별도로 더 안쪼개는 경우가 많다



- # [Attention Is All You Need](https://namu.wiki/w/Attention Is All You Need)

- 로컬 llm 속도
- db 스키마 문서로 정리 하는법
- 무료 gpt api
- llm as a judge



- 벡터 db도 도메인 별로 나눠서
- 보험사 약관 찾아서 설명 해주는 챗봇
- 제조사 장애 내역 문서
- 귀찮은 업무 개선하기(자동화)



- 질문을 받았을때 핵심 키워드로 변경 -> tool로 사용하여 답변 회신 후 반환
- 모르는답변은 확실히 모른다고 하고 해당 내역은 별도 db등에 이력으로 남김(어떤 문의가 있었고, 왜 답변을 못했는지) 추가로 담당자에게 안내하도록 연결





- 이미지로된 pdf 의 경우 캡셔닝 or ocr 사용



- LLM의 모델이 적절한지
- 기술적으로 평가도 있긴하지만 별로 큰 의미는 없을듯? 
- 골든셋 - (일반적인 유즈케이스 질문과 답변)과 LLM의 답변을 비교해서 평가 
  - 유사도 계산
  - 사람이 평가
  - LLM 판단 (평가)
- 경량LLM



- url 도메인도 제한 가능(지침 등으로도)



#### RAG 검증

- 두단계의 걸친 검증
- 검색 결과(CRAG) : 검색 결과 관련성 검증
- 생성된 답변(Self-RAG) : 생성된 답변 검증